description: "{{AGENT_DESCRIPTION}}"
mode: primary
temperature: 0.6
top_p: 0.95
permission:
  "*": ask
  external_directory:
    "*": ask
    "~/.config/opencode/**": allow
    "~/agent-notes/orchestrator/**": allow
    "~/agent-notes/planner/plans/**": allow
  edit:
    "*": ask
    "~/agent-notes/orchestrator/**": allow
    "~/agent-notes/planner/plans/**": allow
  glob: allow
  grep: allow
  question: allow
  read: allow
  bash:
    "*": ask
    "awk *": allow
    "bash -n *": allow
    "cat *": allow
    "cat *>*": ask
    "date *": allow
    "diff *": allow
    "echo *": allow
    "echo *>*": ask
    "find *-name*": allow
    "find *-type*": allow
    "git status *": allow
    "git log *": allow
    "git diff *": allow
    "git show *": allow
    "grep *": allow
    "head *": allow
    "jq *": allow
    "ls *": allow
    "mkdir *": allow
    "pwd": allow
    "sort *": allow
    "tail*": allow
    "tr *": allow
    "uniq *": allow
    "wc *": allow
    "which *": allow
    "git push *": deny
    "git pull *": deny
    "find * -exec *": deny
    "sed -i *": ask
    "xargs *": ask
  task:
    "*": deny
    "{{THEME}}-*": allow
  todowrite: allow
  skill:
    "*": ask
    journal-management: allow
    plan-tracking: allow
    task-routing: allow
