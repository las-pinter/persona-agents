{
  "name": "persona-agents",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc && esbuild dist/index.js --bundle --format=esm --platform=node --outfile=dist/plugin-bundled.js",
    "dev": "tsc --watch"
  },
  "dependencies": {
    "@opencode/plugin": "^2.0.8"
  },
  "devDependencies": {
    "@types/json-schema": "^7.0.15",
    "@types/node": "^25.9.3",
    "esbuild": "^0.25.0",
    "typescript": "^5.0.0"
  }
}
